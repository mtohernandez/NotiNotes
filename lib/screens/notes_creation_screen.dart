import 'package:flutter/material.dart';

class NotesCreationScreen extends StatelessWidget {
  static const routeName = '/note-creation';
  const NotesCreationScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      
    );
  }
}
